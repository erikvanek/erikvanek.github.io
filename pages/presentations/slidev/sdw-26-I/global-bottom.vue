<!-- Copy this file into every new deck's own project root as `global-bottom.vue` - Slidev only auto-discovers global-top.vue / global-bottom.vue at the project root, not inside a theme, so a one-line file per deck is unavoidable. See themes/muni-arts/snippets/global-bottom.vue. -->
<template>
  <CourseProgress />
</template>
