Quick check on the frontend skill before producing the HTML preview, since this is a real frontend artifact going into the repo.Building. Five repo-ready files, all dropping into `pages/dis-kisk/portfolio-shaping/` so a CLI Claude can pick up cold.Five files, all designed to drop into `pages/dis-kisk/portfolio-shaping/` so a CLI Claude (or you) can pick up cold. Reading order if starting from scratch: **handoff.md first**, then whatever it points to.

What I did with the CLI-transferability ask: every doc is self-contained, has a target-location line at the top, a status line, and a last-updated line. The handoff doc carries the orientation — status, locked decisions, pending decisions, next deliverable, working conventions, supporting-docs reading order. It's the single thing a fresh session opens first.

The previous `positioning-v0.1.md` and `case-study-framework-v0.1.md` artifacts are superseded by the clean-named versions in this batch. Discard the old ones.

**On the type pair** — open `type-pairs-preview.html` in a browser to judge. The four pairings render the same content block (a chunk of cile-VI) at real sizes against the locked palette. My recommendation, with reasoning in `type-pairs.md`, is **Pair 02 — DM Serif Display + DM Sans**. It's the only pairing that delivers Hruby-style typographic boldness without leaving the restrained palette, and the editorial register matches the voice cile-VI already has. Defensible alternative is Pair 01 (safer, less memorable). Pair 03 risks projector legibility; Pair 04 risks reading as the wrong era for AI-native work.

**Blocked on, before the deck outline:**

- Type pair pick
- S1 Hackaton vs S2 Ostrovy for the opening beat
- Opening claim wording (draft in `handoff.md`, edit it)
- One or two sharp opinions to plant in the defense

When those land, the deck outline is the next deliverable — and it's where you can either keep going here or hand it off to Claude Code with the handoff doc as the entry point. Either way the next session starts in the same place.